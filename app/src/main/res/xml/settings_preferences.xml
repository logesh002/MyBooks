<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory app:title="Appearance">

        <ListPreference
            app:key="theme_preference"
            app:title="Theme"
            app:summary="Choose your app's theme"
            app:entries="@array/theme_entries"
            app:entryValues="@array/theme_values"
            app:defaultValue="SYSTEM" />

    </PreferenceCategory>

    <PreferenceCategory app:title="Data Management">

        <Preference
            app:key="reset_database"
            app:title="Reset Database"
            app:summary="Deletes all saved books and tags. This action cannot be undone." />

        <Preference
            app:key="export_data"
            app:title="Export Library to CSV"
            app:summary="Save all your books to a CSV file." />

        <Preference
            app:key="import_data"
            app:title="Import Library from CSV"
            app:summary="Load books from a previously exported CSV file." />

    </PreferenceCategory>

    <PreferenceCategory app:title="Defaults">

        <ListPreference
            app:key="default_status_preference"
            app:title="Default status for new books"
            app:summary="Sets the initial status when adding a new book."
            app:entries="@array/status_entries"
            app:entryValues="@array/status_values"
            app:defaultValue="FOR_LATER" />

        <ListPreference
            app:key="default_format_preference"
            app:title="Default format for new books"
            app:summary="Sets the initial format when adding a new book."
            app:entries="@array/format_entries"
            app:entryValues="@array/format_values"
            app:defaultValue="PAPERBACK" />

    </PreferenceCategory>

    <PreferenceCategory app:title="Home Screen">

        <Preference
            app:key="chip_order_preference"
            app:title="Home screen filter order"
            app:summary="Set the display order for the status filter chips." />

    </PreferenceCategory>
    <PreferenceCategory app:title="Online Search">

        <ListPreference
            app:key="online_search_api"
            app:title="Search Provider"
            app:summary="Choose which service to use for online book search."
            app:entries="@array/api_provider_entries"
            app:entryValues="@array/api_provider_values"
            app:defaultValue="OPEN_LIBRARY" />

    </PreferenceCategory>

    <PreferenceCategory app:title="About">

        <Preference
            app:key="app_version"
            app:title="App Version" />

    </PreferenceCategory>

</PreferenceScreen>